brew "git"
brew "mise"
# mn-post reads disciplinary order PDFs: poppler (pdfinfo/pdftotext/pdftoppm)
# for the text layer and page renders, tesseract to OCR scanned pages.
brew "poppler"
brew "tesseract"

if OS.mac?
  cask "docker-desktop"
end
