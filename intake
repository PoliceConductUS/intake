#!/usr/bin/env sh
intake_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export TMPDIR="${TMPDIR:-$intake_dir/.cache/tmp}"
mkdir -p "$TMPDIR"
exec node --import "$intake_dir/node_modules/tsx/dist/loader.mjs" "$intake_dir/src/cli.ts" "$@"
