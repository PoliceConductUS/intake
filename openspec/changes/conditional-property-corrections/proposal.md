# Conditional property corrections and distinct Census places

Extend `cache set` with `--from <existing-value>` for a persistent exact-value correction, and permit every property in the entity schema. Without `--from`, a supplied value fills an absent/null input. Preserve existing resolved-property corrections and automatic fingerprinted caching. Source-addressed corrections need no canonical mapping and live in the durable workspace, with canonical envelope IO, command ID, timestamp, and previous entries.

Apply input corrections before downstream derivation. Census keys locations and their boundaries by geography type plus GEOID. Alternate-county alias keys combine the place key and county key. Generation applies shared corrections to typed records before resolving names, parent references, and derived paths. Transforms receive no correction hook. Census must never merge distinct GEOIDs because their paths collide. Unresolved collisions fail with both source identities. Each place retains its own geometry. For the four audited conflicts, persist label corrections for the smaller ALAND entity through the CLI, retaining the common-name path for the larger entity. No record-specific code exceptions.

No schema migration, acquisition, reset, or direct database write. Existing Census artifacts must be transformed once for the new source-key contract. Existing canonical IDs are retained by explicitly migrating durable workspace mappings before generation; runtime resolution does not accept the old key contract. Geometry validity repair and land-only canonical-county ranking remain separate audit findings.
