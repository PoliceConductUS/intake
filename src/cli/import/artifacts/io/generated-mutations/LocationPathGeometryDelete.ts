/* eslint-disable */
// Generated by scripts/generate-envelope-types.ts. Do not edit.
import path from "node:path";
import { z } from "zod";
import { INTAKE_API_VERSION } from "../../../../../shared/io/import-types.js";
import {
  firstIssuePath,
  yamlDigest,
  yamlResourcePath,
} from "../../../../../shared/io/resource.js";
import {
  readYamlDocumentFile,
  writeYamlDocumentFile,
} from "../../../../../shared/io/internal/yaml-document.js";

type EnvelopeReadRef =
  | { path: string; kind?: string; sha256?: string }
  | { ref: { path: string; kind?: string; sha256?: string } };

type EnvelopeReadOptions = {
  expectedNamespace?: string;
  relativeTo?: string;
};

function formatError(error: z.ZodError): string {
  return `LocationPathGeometryDelete is malformed at ${firstIssuePath(error)}.`;
}

function refValue(pathOrRef: string | EnvelopeReadRef): {
  path: string;
  kind?: string;
  sha256?: string;
} {
  if (typeof pathOrRef === "string") {
    return { path: pathOrRef };
  }
  if ("ref" in pathOrRef) {
    return pathOrRef.ref;
  }
  return pathOrRef;
}

function resolveReadPath(
  pathOrRef: string | EnvelopeReadRef,
  options: EnvelopeReadOptions,
): { filePath: string; kind?: string; sha256?: string } {
  const ref = refValue(pathOrRef);
  if (typeof pathOrRef === "string" || path.isAbsolute(ref.path)) {
    return { ...ref, filePath: ref.path };
  }
  if (
    options.relativeTo === undefined ||
    options.relativeTo.trim().length === 0
  ) {
    throw new Error(
      `Relative ${ref.kind ?? "LocationPathGeometryDelete"} ref requires relativeTo.`,
    );
  }

  const baseDirectory = path.dirname(options.relativeTo);
  const resolvedPath = path.resolve(baseDirectory, ref.path);
  const relativePath = path.relative(baseDirectory, resolvedPath);
  if (relativePath.startsWith("..") || path.isAbsolute(relativePath)) {
    throw new Error(
      `${ref.kind ?? "LocationPathGeometryDelete"} ref.path escapes its directory: ${ref.path}`,
    );
  }
  return { ...ref, filePath: resolvedPath };
}

const metadataSchema = z
  .object({
    name: z.string().trim().min(1),
    namespace: z.string().trim().min(1),
    labels: z.record(z.string(), z.string()).optional(),
    annotations: z.record(z.string(), z.string()).optional(),
  })
  .strict();

export const specSchema = z.object({}).strict();

export const schema = z
  .object({
    apiVersion: z.literal(INTAKE_API_VERSION),
    kind: z.literal("LocationPathGeometryDelete"),
    metadata: metadataSchema,
    spec: specSchema,
  })
  .strict();

export type LocationPathGeometryDeleteEnvelope = z.infer<typeof schema>;
export type LocationPathGeometryDeleteInput = Omit<
  LocationPathGeometryDeleteEnvelope,
  "apiVersion" | "kind"
>;

function parseLocationPathGeometryDelete(
  value: unknown,
): LocationPathGeometryDeleteEnvelope {
  const result = schema.safeParse(value);
  if (!result.success) {
    throw new Error(formatError(result.error));
  }
  return result.data;
}

function newLocationPathGeometryDelete(
  input: LocationPathGeometryDeleteInput,
): LocationPathGeometryDeleteEnvelope {
  return parseLocationPathGeometryDelete({
    apiVersion: INTAKE_API_VERSION,
    kind: "LocationPathGeometryDelete",
    ...input,
  });
}

async function readLocationPathGeometryDelete(
  pathOrRef: string | EnvelopeReadRef,
  options: EnvelopeReadOptions = {},
): Promise<LocationPathGeometryDeleteEnvelope> {
  const ref = resolveReadPath(pathOrRef, options);
  if (ref.kind !== undefined && ref.kind !== "LocationPathGeometryDelete") {
    throw new Error(
      `LocationPathGeometryDelete ref.kind ${ref.kind} does not match expected kind LocationPathGeometryDelete: ${ref.filePath}`,
    );
  }
  const { contents, document } = await readYamlDocumentFile(
    ref.filePath,
    "LocationPathGeometryDelete",
  );
  if (ref.sha256 !== undefined && yamlDigest(contents) !== ref.sha256) {
    throw new Error(
      `LocationPathGeometryDelete sha256 mismatch: ${ref.filePath}`,
    );
  }
  const envelope = parseLocationPathGeometryDelete(document);
  if (
    options.expectedNamespace !== undefined &&
    envelope.metadata.namespace !== options.expectedNamespace
  ) {
    throw new Error(
      `LocationPathGeometryDelete namespace ${envelope.metadata.namespace} does not match expected namespace ${options.expectedNamespace}: ${ref.filePath}`,
    );
  }
  return envelope;
}

async function writeLocationPathGeometryDelete(
  directory: string,
  envelope: LocationPathGeometryDeleteEnvelope,
): Promise<{ path: string }> {
  const parsed = parseLocationPathGeometryDelete(envelope);
  const filePath = yamlResourcePath(directory, parsed);
  await writeYamlDocumentFile(filePath, parsed);
  return { path: filePath };
}

export const LocationPathGeometryDelete = {
  kind: "LocationPathGeometryDelete",
  schema,
  new: newLocationPathGeometryDelete,
  read: readLocationPathGeometryDelete,
  write: writeLocationPathGeometryDelete,
};
